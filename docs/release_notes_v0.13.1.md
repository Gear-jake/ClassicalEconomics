# v0.13.1 发布说明（2026-08-19）

## 内存生命周期修复

- 世界切换或返回主菜单时释放静态缓存中的旧 `City`、`Actor` 和 `Kingdom` 引用。
- 贸易边缓存同步淘汰已消失城市及王国的边，避免长期游戏中的缓存增长。
- 后台贸易任务真正退出前禁止复用计算缓冲，避免跨世界任务并发持有和读写同一数据。
- 清理已消失王国的灾害人口/城市快照和时代状态。
- 防止热更新重复创建 `EconomyTickRunner`、经济工具栏和 tooltip 引用。
- 修正本地构建脚本的失效绝对路径，确保发布 DLL 来自当前源码。

## 验证

- Roslyn 全量编译通过。
- `git diff --check` 通过。
- 发布包只包含运行资源和多语言 README，不包含源码、构建日志或本机配置。
