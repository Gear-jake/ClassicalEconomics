# v0.94 发布说明（可安装候选包已生成）

> 状态说明：概览页移除人口指标卡；44/44 门禁 + performance_audit + 构建全绿。F3 实机验收由用户安装到游戏后亲自执行。

## 本版本变更

- **概览页不再显示"人口"指标卡**：配合人口倍数类模组时单位数会被成倍放大，数字失真且出戏（用户反馈）。指标卡现为 GDP / 人均 / 基尼 / 贸易 / 泡沫 五项。
- 同步移除四语言 `stat_pop` 键（避免新增孤儿键）；各语言文件现为 346 键。
- 人口数据本身仍正常采集与统计（`AliveActorCount` 供富豪榜副标题、快照与引擎逻辑使用），仅 UI 展示移除。

## 构建与哈希验证

- `build_local.ps1` 全量构建通过（exit 0，`EconomyMod.dll` 228864 字节）。
- 暂存包 `release\ClassicalEconomics-0.94\EconomyMod\` 与 `bin\EconomyMod.dll`、zip 内 DLL 三方 SHA-256 一致（哈希见打包记录）。

## 继承 v0.93 的全部内容

- 窗口标题切语言即时刷新（四个悬浮窗统一接线）+ 全部玩家可见文本四语言覆盖（18 处横幅、指标卡、事件流描述、工具栏单一真相源）+ 新门禁 Test-LocalizationCoverage。

## 暂缓任务

- 无。F3 实机验收由用户安装后亲自执行。
